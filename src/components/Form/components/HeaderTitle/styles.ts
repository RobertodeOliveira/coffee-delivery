import styled from 'styled-components'

const InfoForm = styled.div`
  display: flex;
  gap: 0.5rem;
`

const Container = styled.div`
  display: flex;
  flex-direction: column;
  gap: 2px;
`

export { InfoForm, Container }
